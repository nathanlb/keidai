apiVersion: v2
name: keidai
description: Keidai agent ecosystem (Fuda, Torii, Shaiden, keidai-ui) for self-hosted Kubernetes
type: application
# Chart version == appVersion == image tags (platform semver). Published to
# oci://ghcr.io/<owner>/keidai on the matching v* git tag.
version: 0.5.0
appVersion: "0.5.0"
kubeVersion: ">=1.25.0-0"
home: https://github.com/nathanlb/keidai
sources:
  - https://github.com/nathanlb/keidai
