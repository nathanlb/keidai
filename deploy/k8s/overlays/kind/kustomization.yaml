apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

# Local kind: images are `kind load`ed; never pull from a registry.
patches:
  - path: patch-images.yaml
  - path: patch-keidai-ui-hostport.yaml
