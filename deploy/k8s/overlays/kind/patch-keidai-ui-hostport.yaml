apiVersion: apps/v1
kind: Deployment
metadata:
  name: keidai-ui
  namespace: keidai
spec:
  template:
    spec:
      containers:
        - name: keidai-ui
          ports:
            - containerPort: 3000
              hostPort: 3000
              name: http
