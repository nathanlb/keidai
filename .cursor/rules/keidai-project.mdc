---
description: Keidai monorepo sources — Linear tasks and Notion docs
alwaysApply: true
---

# Keidai project context

Keidai is the agent ecosystem monorepo. Day-to-day local dev uses Docker Compose. In-cluster bring-up (projected k8s SA subject tokens, BFF-only edge) lives under `deploy/k8s/` (kind default; OrbStack overlay) — see `deploy/k8s/README.md`.

## Ecosystem components

- **Torii — MCP Gateway** (`apps/torii`, `:3100`) is the gateway and control plane. One MCP endpoint fronting N backends; resolves credentials per server (`user_oauth` / `service_key` / `none`) so they never reach the agent; enforces group-based tool policy that fails closed on unknown groups; gates selected tools behind human approval (group-level gates in Postgres); emits `CallTrace` records.
- **Fuda — Agent Identity Provider (AIdP)** (`apps/fuda`, `:3300`) is a standalone service with its own SQLite registry of agents, bearers, grants, and versioned personas. Exchanges a subject token for a short-lived agent JWT (`POST /token`) and publishes JWKS. Torii validates those JWTs offline and calls Fuda for nothing else.
- **Shaiden — Agent Runtime** (`apps/shaiden`, `:3200`) drives tasks to a typed termination outcome via a thin model/tool loop over Torii's MCP session, persists tasks and runs in SQLite, and parks a run while it waits on an approval decision.
- **keidai-ui** (`apps/keidai-ui`, `:3000`) is the React + Vite operator frontend and BFF: serves the SPA and reverse-proxies `/api/*` (and Torii `/oauth/callback/*`) to Torii/Fuda/Shaiden. Compose and kind both publish only the BFF as the operator edge.

Naming and boundary details that are easy to get wrong:

- The user-OAuth credential strategy is **`user_oauth`**, not `delegated_connection`.
- Group policy is authored in keidai-ui and stored in Torii Postgres — not in `torii.yaml`. Per-server allow/deny/gated lists in the Connections UI are derived projections of persisted group policy.
- Torii has exactly **one** identity resolver, `FudaJwtIdentityResolver`, permanently. Platform swappability lives in Fuda's `SubjectTokenValidator`.
- **Torii owns the approval gate and ledger**; Shaiden only parks and resumes.
- Compose/local attestation uses a static shared secret (`SHAIDEN_BEARER` / `FUDA_STATIC_SUBJECT_TOKEN`). In-cluster, Shaiden presents a projected SA token (`SHAIDEN_SUBJECT_TOKEN_FILE`) and Fuda validates via `FUDA_K8S_SA_OIDC_*`. Allowed subjects always resolve to the platform bearer `shaiden-runner`.
- Operator Google ↔ opaque `owner_id` lives in `deploy/operators.example.yaml` (Compose mount / k8s ConfigMap). There is no `KEIDAI_OWNER_ID` or Torii `boot_owner_id`.
- Compose and kind publish only the keidai-ui BFF (`:3000`); set `TORII_GATEWAY_BASE_URL` to that origin for OAuth callbacks.
## Implementation tasks (Linear)

Stories and acceptance criteria live in the following Linear projects:
- **[Torii](https://linear.app/nathanlb/project/torii-c257f2b8953d/issues)**
- **[Shaiden](https://linear.app/nathanlb/project/shaiden-7568ee5d79af/issues)**
- **[Fuda](https://linear.app/nathanlb/project/fuda-d1656834a613/issues)**

Issue IDs use team prefix **`NAT-`** (e.g. `NAT-11` = Story 4b, `NAT-12` = Story 4c).

### Implementing a Linear issue

When asked to implement, build, or work on a specific Linear issue by ID:

1. **Fetch the issue** via Linear MCP `get_issue` (e.g. `NAT-12`).
2. **Check out the branch** — use `gitBranchName` from the response (same name Linear's "copy branch name" provides). If the current branch is not `gitBranchName`:
   - Check out the branch if it already exists locally or on `origin`.
   - Otherwise create it from `main` (or the base branch the user specified).
3. **Implement** per the issue description and acceptance criteria.

Do not skip the branch step or invent a branch name.

## Product & design docs (Notion)

Platform vision and component boundaries:

https://app.notion.com/p/38307ec181ff815b8276d59d005fd612

Torii gateway — product doc, plus child pages for architecture, credential resolution, and stack:

https://app.notion.com/p/38307ec181ff80e49dd8ff384139f8b2

Fuda agent identity provider:

https://app.notion.com/p/38307ec181ff81529d0dee3c0c09238f

Shaiden agent runtime:

https://app.notion.com/p/38307ec181ff81d8833cf4e05f6b8437

keidai-ui frontend:

https://app.notion.com/p/38507ec181ff81b38d8df7349de05381

Torii project card (strategy, positioning, landscape scan — not architecture):

https://app.notion.com/p/36c07ec181ff813d8f34f9b0e617de34
