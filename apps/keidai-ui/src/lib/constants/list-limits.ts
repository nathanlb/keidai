/** Rows shown per table page (matches Activity & Traces). */
export const TABLE_PAGE_SIZE = 50;

/** Max items kept in client list buffers (matches trace/run retention). */
export const LIST_BUFFER_LIMIT = 200;
