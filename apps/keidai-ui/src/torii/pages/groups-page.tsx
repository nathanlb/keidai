export function GroupsPage() {
  return (
    <p className="max-w-xl text-[13.5px] leading-relaxed text-muted-foreground">
      Author which tools each group may use. This catalogue is coming next —
      group policy already fails closed on unknown groups at the gateway.
    </p>
  );
}
