# =============================================================================
# Demo agent — copy to apps/demo-agent/.env
# DEMO_AGENT_BEARER can live in the repo root .env instead (shared across apps).
# Do not commit .env.
# =============================================================================

ANTHROPIC_API_KEY=
DEMO_OWNER_EMAIL=you@example.com
TORII_MCP_URL=http://127.0.0.1:3100/mcp
# DEMO_MODEL_ID=claude-sonnet-4-5-20250929

# Only needed here if not set in repo root .env
# DEMO_AGENT_BEARER=
