# Demo agent

Minimal AI SDK agent that runs the [open-torii status digest](../docs/demo.md) scenario against a local Torii gateway.

See **[docs/demo.md](../docs/demo.md)** for prerequisites, environment setup, OAuth linking, and step-by-step run instructions.

```bash
pnpm demo          # from repo root (gateway must be running)
```
